<body>
  <%= render "shared/header" %>

  <%= render "shared/navbar" %>
  
  <div id="content">
    <div id="content_text">
      <% @playlistrequests.each do |playlistrequest| %>
        playlistrequest.title
      <% end %>
    </div>
  </div>

  <%= render "shared/footer" %>

</body>